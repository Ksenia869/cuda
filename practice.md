# Парктика

## Часть 1

В данной части практике будем рассматривать библиотеку [cupy](https://cupy.dev). **Цель** данной части практики рассмотреть простой способ вычисления с помощью gpu и сравнения перформанса с аналогичной реализацией на numpy.

Результатом данной практики должен быть оформленный отчет. В нём для каждого задания должны быть приведены: условие, реализация задания на двух фреймворках (numpy, cupy), таблица, в которой приведено сравнение времени работы этих двух реализаций и график, являющийся визуализацией вышеописанной таблицы.

### Задание 1.

Поэлементные операции с большими массивами.

На вход функции подаётся массив размерностью $N$. Необходимо реализовать вычисление выражения $y = \sqrt{x}+sin(x)⋅exp(−x)$ (используйте встроенные векторные функции, обычный for не даст вам ускорения). Для экспериментов ограничим значение, которое может принимать $N$ в задачах следующим интервалом [1, 10000000].

### Задание 2.

Расчет статистик.

На вход функции подаётся квадртаная матрицы $A$ размерностью $N \times N$. Необходимо реализовать функцию рассчета статистик (среднее значение, медиана, мода, дисперсия, среднеквадартаичное отклонение, первый и третий квартиль) для каждой колонки матрицы. Для экспериментов ограничим значение, которое может принимать $N$ в задачах следующим интервалом [1, 100000].

### Задание 3.

Матричное умножение.

На вход функции подаётся две квадртаная матрицы $A$ и $B$ размерностью $N \times N$. Необходимо реализовать вычисление их произведения $C = A \cdot B$. Для экспериментов ограничим значение, которое может принимать $N$ в задачах следующим интервалом [1, 4096].

### Задание 4.

Ряд Маклорена.

На вход подается массив размерностью $N$ со значениями [-10, 10] и число $M$ - количество многочленов для апрокимации. Необходимо реализовать функцию, которая вычисляет разложение функции $sin(x)$ в ряд Маклорена для заданного массива и количество многочленов для апрокимации. Для экспериментов ограничим значение, которое может принимать $N$ в задачах следующим интервалом [1, 10000000], а $M$ - одним, любым значением, но не меньше 5.

## Часть 2.

В данной части практике будем рассматривать библиотеку [numba](https://nvidia.github.io/numba-cuda/). **Цель** данной части практики получше познакомиться с концепциями и сложностями выичсления на cuda.

Результатом данной практики должно быть выполненное нижеописанные масштабное задание. Задание должно быть оформленно в виде кода на python либо как проект, в случае если у студента есть своё устройство с gpu, либо как jupyter тетрадка на colab в ином случае. 

Пусть финальные скрипты для запуска обучения и прогноза модели будут оформлены в тетрадку, где вы должны залоггировать, что: 
1. Модель действительно обучается.
2. Есть различия между временем вычисление на cpu и gpu.

### Задание.

Реализовать небольшую свёрточную нейронную сеть для задачи бинарной классификации, которая может проводит вычисления и обучаться как на cpu (с использованием numpy), так и на gpu (с использованием numba).

![Пример простейшей нейронной сети](images/convolution-01.jpg.webp)

### Инструкция по выполнению задания.

Далее приведена небольшая инструкция по выполнению работы. Для успешного окончания курса не обязательно досконально следовать ей. Она всего лишь призвана направить студента в нужноую сторону при реализации.

В качестве референса для воплнения задания предлагается (но не настаивается) взять интерфейс взаимодействия с моделями в pytorch.

Стоит отметить, что для начала рекомендуется выполнить данное задание на numpy, а уже дальше интегрировать numba.

#### Шаг 1.

Реализовать класс `Model`, который будет из себя представлять следующий вид:

```
class Model:
    def __init__(self):
        self.layers = []
    
    def predict(self, X):
        """Прямой проход, возвращает выход и кэш активаций"""
        y = X
        cache = []
        for layer in self.layers:
            y, c = layer.forward(y)
            cache.append(c)
        return x, cache
    
    def _get_grads(self, dL_dy, cache):
        """Обратный проход, возвращает градиенты"""
        dy = dL_dy
        grads = []
        for layer in self.layers[::-1]:
            dy, grad = layer[i].backward(dy, cache[layer_name])
            grads.append(grad)
        return grads[::-1]

    def _update_weights(self, upd_vals):
        for i in range(len(self.layers)):
            self.layers[i].weights += upd_vals
            

    def save_model(self, path):
        """Сохранение о слоях и их весах на диск."""
        pass

    @classmethod
    def from_file(self, path):
        """Загрузка модели с диска."""
        pass
```

Реализовать класс `BaseLayer`, от которого будут наследоваться слои для вашей NN:

```
class BaseLayer:
    def __init__(self, weight=None, **kwargs):
        self.weights = self._init_weights() if weight is None else weights

    def forward(self, X):
        """Прямой проход, возвращает выход и кэш активаций"""
        pass
    
    def backward(self, dL_dy, cache):
        """Обратный проход, возвращает градиенты и dL_dx"""
        pass
```

#### Шаг 2.

Реализовать основные слои для CNN с использованием numpy и numba: `Conv2DLayer`, `ReLULayer`, `LinearLayer`, `MaxPoolLayer`, `SoftmaxLayer`, `FlattenLayer` и заполнить для них метод forward и протестировать, что он корректно работает на небольших значениях.

#### Шаг 3.

Для каждого слоя реализовать расчёт производной для метода backward. Так же протестировать его на небольших значениях.

#### Шаг 4.

Реализовать функцию потерь для классификации (кросс энтропия (или её более простую версию: лог лосс)) с помощью двух классов `BaseLossFunction` и `CrossEntropyLoss` (`LogLoss`):

```
class BaseLossFunction:
    def __init__(self):
        pass

    def calc(self, y_true, logits):
        pass

    def derivative(self, y_true, logits):
        pass

class CrossEntropyLoss:
    `Здесь будет ваша логика`
```

#### Шаг 4.

Реализовать оптимизатор с помощью двух классов: `BaseOptimizer` и его наследника `GDOptimizer` (классический градиентный спуск). По желанию можно так же реализовать модификации градиентного спуска ввиде отдельных классов (настоятельно рекомендую ещё реализовать `SGDOptimizer`, т.к. его реализация простая, а за счет него вы ускорите тестирование).

```
class BaseOptimizer:
    def __init__(self, loss_func, **kwargs):
        self.loss_func = loss_func

    def optimize(self, logits, cache, model):
    """Запускает оптимизацию модели."""
        pass

class GDPtimizer(BaseOptimizer):
    def __init__(
        self,
        loss_func,
        learning_rate = 0.1,
        **kwargs,
    ):
        super.__init__(loss_func, **kwargs)
        self.learning_rate = learning_rate

    def optimize(self, logits, cache, model):
    """Запускает оптимизацию модели."""
        dL_dy = self.log_loss.derivative(y_true, logits)

        grads = model._get_grads(dL_dy, cache)
        model._update_weights(model, -self.learning_rate * grads)
```

#### Шаг 5.

Собрать всё воедино, сделав два скрипта: с обучением модели и её инференсом. 

#### Шаг 6.

Переписать numpy реализацию вычислений на numba. 
